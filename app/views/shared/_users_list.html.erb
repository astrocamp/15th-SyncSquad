<div id="user-list" class="flex items-center flex items-center justify-between sticky top-0">
  <h4 class= "text-xl p-4 w-full shadow-lg border-b border-l bg-gray-200"> <%= t('rooms.contacts') %> </h4>
</div>
<%= turbo_stream_from "users" %>
<div id="users">
  <%= render @users %>
</div>