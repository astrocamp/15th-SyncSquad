<div class="cont-<%= message.user.id %>">
  <div class="msg-<%= message.user.id %> mt-5 rounded-xl">

    <div class="flex content-center gap-2 mb-2">
      <%= image_tag(message.user.chat_avatar, class: "w-12 h-12 rounded-full") %>
      <div>
        <div> <%= message.user.email.split("@").first.split("-").reverse.map(&:capitalize).join(" ") %></div>
      </div>
    </div>

    <div class="markdown">
      <%= raw advanced_markdown(message.body) %>
    </div>
  </div>
</div>  
