<nav class="border-gray-200 bg-gray-50">
  <div class="flex flex-wrap items-center justify-between px-4 py-2 mx-auto md:px-6">
    <h1><%= link_to  "SyncSquad",
            root_path , 
            class: "block tracking-wide self-center text-xl italic whitespace-nowrap font-bevan" %>
    </h1>
    <div class="flex items-center">
      <% if user_signed_in? %>
        <div class="hidden md:block" id="navbar-solid-bg">
          <ul class="flex text-sm font-medium rounded-lg bg-gray-50 md:space-x-8 rtl:space-x-reverse md:flex-row md:border-0 md:bg-transparent ">
            <li>
              <%= link_to t('main.chatroom'), rooms_path,
                  class: 'block px-3 py-1 text-gray-900 rounded md:p-0 hover:bg-gray-100 md:hover:bg-transparent md:border-0 md:hover:text-blue-700 ' %>
            </li>
            <li>
              <%= link_to t('main.project'), projects_path,
                  class: 'block px-3 py-1 text-gray-900 rounded md:p-0 hover:bg-gray-100 md:hover:bg-transparent md:border-0 md:hover:text-blue-700 ' %>
            </li>
          </ul>
        </div>
      <% end %>
      <div class="mx-4"><%= render 'layouts/lang_selector' %></div>
      <%= render 'layouts/photo_dropdown' %>
    </div>
  </div>
</nav>
