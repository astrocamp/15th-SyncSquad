<div class="absolute right-0 px-5 py-2 top-10">
    <% [alert, notice, success, info, mail].each do |message| %>
        <%= render partial: "shared/#{message}", locals: { message: }  if message %>
    <% end %>
</div>