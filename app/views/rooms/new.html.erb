<%= turbo_frame_tag "modal", class: "modal-backdrop hidden" do %>
  <%= render "shared/new_room_modal",
             new_room: @room if params[:modal_type] == "public" %>

  <%= render "shared/new_group_modal",
             new_private_group: @room if params[:modal_type] == "private" %>
<% end %>